export const DEFAULT_SETTINGS = {
  enabled: true,
  allowOrigin: "*",
  customOrigin: "",
  allowMethods: "GET, POST, PUT, DELETE, PATCH, OPTIONS",
  allowHeaders: "*",
  allowCredentials: true,
  domains: ["*"],
  removeExisting: true
};